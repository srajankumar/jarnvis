import React from "react";

const Note = () => {
  return (
    <div className="text-sm text-start pt-2 tracking-wide  text-stone-400">
      <span className="underline underline-offset-2">Note</span>: We are using
      open source flan-t5-base language model.
    </div>
  );
};

export default Note;
